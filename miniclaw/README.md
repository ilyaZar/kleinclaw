# miniclaw

TypeScript Kleinanzeigen publishing core for KleinClaw.

This embedded runtime is packaged inside the KleinClaw OpenClaw plugin. It
contains deterministic model behavior plus browser-backed command flows that
stay gated by the plugin and miniclaw CLI.
